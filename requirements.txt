# Sentinel Desktop — AI-powered desktop automation
#
# As of v18, this file is a THIN POINTER. The single source of truth for dependencies
# is pyproject.toml ([project.dependencies] + [project.optional-dependencies]).
#
# Install everything (all subsystem features on supported platforms):
#     pip install -e ".[all]"
#
# Install a specific feature group:
#     pip install -e ".[web]"        # Playwright browser automation
#     pip install -e ".[netops]"     # SSH / network device control
#     pip install -e ".[voice]"      # STT / audio capture
#     pip install -e ".[mcp]"        # MCP server entry point
#     pip install -e ".[net]"        # advanced DNS diagnostics
#     pip install -e ".[mfa]"        # TOTP / MFA handling
#     pip install -e ".[ocr]"        # OCR (click_text / read_text)
#     pip install -e ".[windows]"    # Windows-only: UIA, pywin32, keyboard, tray, pycaw
#
# CI (lint + tests):
#     pip install -e ".[dev]"
#
# NOTE: the [web] extra installs the playwright Python package but NOT the browser
# binaries. After installing, also run:  playwright install
-e .[all]
